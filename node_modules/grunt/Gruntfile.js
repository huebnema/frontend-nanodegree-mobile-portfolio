concat: {
    dist: {
        src: [
            'js/*.js', // All JS in the libs folder
            'views/js/*.js', // All JS in the libs folder
        ],
        dest: 'js/build/production.js',
    }
}